# Work log 2026-09-24

## Done

- Reviewed the PRD and settled open decisions with the owner:
  - lowercase `codeit` naming
  - repo at `~/projects/codeit` on the WSL filesystem
  - Docker Desktop with WSL integration
  - M4.5 sandbox app
  - per-ticket clones
  - Next.js dashboard
- Installed the WSL toolchain: uv 0.12.19, nvm with Node 24.21 LTS, Claude Code 2.1.282.
- Created the repo, moved the PRD to `docs/prd.md` and applied the rename and fixes (v1.1). Recorded ADRs 0001 to 0003.
- M0 scaffold:
  - `pyproject.toml` (uv)
  - Typer CLI with `config validate`, `db upgrade` and milestone stubs
  - Pydantic config models
  - structlog JSON logging
  - SQLAlchemy models for all PRD 13 tables, with an Alembic baseline and WAL mode
  - CI workflow, pre-commit (gitleaks, ruff)
  - placeholder folders
- Checks: ruff and mypy --strict clean. 20 tests pass, 1 live test skipped, coverage 96%.

## Blocked on owner

- Docker Desktop: start it and enable WSL integration for Ubuntu-24.04.
- `sudo apt install jq gh` (sudo needs a password).
- GitHub repo `codeit` so CI can run.

## Next

- M1: Jira client, ADF converter, `codeit jira doctor` / `discover`.
