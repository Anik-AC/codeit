# Work log 2026-09-25

## Done

- **M0 (Scaffold) is complete** on branch `m0-scaffold`. Local checks:
  - `codeit --help` works.
  - `codeit config validate` rejects a malformed config.
  - ruff and mypy --strict are clean.
  - 20 tests pass and 1 live test is skipped; coverage is 96%.
- CI has not run yet: there is no GitHub remote.
- Added `docs/plan.md`, a copy of the approved project plan. The M4.5 row was aligned with the PRD: the steering kit is added in M5 by `codeit init-target`.
- Added `CLAUDE.md` with session start instructions.

## Pending on the owner

- [ ] Start Docker Desktop and enable **Settings > Resources > WSL Integration > Ubuntu-24.04**.
- [ ] `sudo apt install -y jq gh`, then `gh auth login`.
- [ ] Create the GitHub repo `codeit`, so the `m0-scaffold` branch can be pushed, the M0 PR opened and CI run.
- [ ] Review and merge the M0 PR.
- [ ] Before M1: set up the `CODEIT` Jira project with the statuses, custom fields and board estimation from PRD 7.1. Until a bot account exists, the owner's own API token is fine (ADR-0001).
- [ ] Decide whether to delete the original `PRD.md` in the Windows folder. `docs/prd.md` is the canonical copy.

## Next

- M1: Jira client, ADF converter, `codeit jira doctor` / `discover`. Starts after M0 is confirmed.

## Session 2 (resumed from `~/projects/codeit`)

### Done

- Environment check:
  - Working: Docker Desktop 4.71.0 (engine 29.4.1) from WSL, uv 0.12.19, Node 24.21.0, Claude Code 2.1.282.
  - Still missing: `gh` and `jq`.
  - The repo has no git remote.
- PRD v1.2: jira-mcp now runs on the host over streamable HTTP. Each run gets a short-lived bearer token bound to its role and ticket, and no Jira token enters a worker container. The Claude OAuth token in containers is listed as an accepted risk (19.7). Recorded in ADR-0004.
- Removed the Jira site from `sandbox.egress_allowlist` in `config/config.yaml`. Annotated `.env.example`.

### Pending on the owner

- [ ] `sudo apt install -y gh jq`, then `gh auth login`.
- [ ] Create the GitHub repo `codeit` (empty, no README), then push `m0-scaffold` and open the M0 PR.
- [ ] Review and merge the M0 PR, including the PRD v1.2 and ADR-0004 changes.
- [ ] Before M1: set up the `CODEIT` Jira project (PRD 7.1).

## Session 3: M1 (Jira client)

### Done

- M0 merged (PR #1). Created the public repo `Anik-AC/codeit`; gitleaks scanned the full history first and found nothing.
- M1 on branch `m1-jira-client`:
  - `src/codeit/jira_client/`: client (retries, `Retry-After`, error types), ADF converter, search with `nextPageToken` and both guards, issues, transitions with cache and refetch, comments, discover, doctor
  - `codeit jira doctor [--write-test]` and `codeit jira discover`
  - ADR-0005 for decisions beyond the PRD
- Checks: 170 unit tests pass, and `jira_client` coverage is 97 to 100% per module. ruff and mypy --strict are clean.
- Owner set up the `CODEIT` Jira project and `.env`. The first doctor run failed on transitions for two reasons. First, the new statuses lacked "allow all" (the owner fixed this). Second, doctor sampled a Subtask, which is on Jira's default workflow. Discovery and doctor now use the Story workflow only (ADR-0005).
- `codeit jira doctor --write-test` passes all 6 checks, and `codeit jira discover` wrote 7 statuses and 7 transitions.
- `LIVE=1`: all 6 live tests pass (doctor, transition to every status, link direction, ADF through Jira, custom fields, token paging). **M1 acceptance met.**

### Pending on the owner

- [ ] Protect `main` on GitHub.
- [ ] Review and merge the M1 PR once the live tests pass.

## Session 4: M2 (jira-mcp)

### Done

- M1 merged (PR #2).
- M2 on branch `m2-jira-mcp`:
  - `mcp_servers/jira/`: MCP SDK 2.2 `MCPServer` with six tools; role filtering (hidden tools are invisible and refused); SDK bearer auth with run tokens; stdio for the owner
  - `src/codeit/run_tokens.py` with the `mcp_tokens` table (migration 0002): hashed, bound to role, ticket and run, expiring, revocable
  - CLI: `codeit mcp serve [--stdio]`, `codeit mcp token`, `codeit mcp revoke`; console script `codeit-jira-mcp`
  - Fixed an M0 bug: `alembic_version` was never committed, so a second `codeit db upgrade` failed
  - ADR-0006, PRD v1.3 (12.6, 13, 15, 20.1), README section
- Checks: 238 unit tests pass. `LIVE=1`: 8/8 pass, including `claude -p` fetching a ticket over HTTP with a coder token, and stdio `get_ticket` on the real site. The MCP Inspector CLI smoke test was run and documented.

### Pending on the owner

- [ ] `main` protection: the `Protect_Main` ruleset exists but is **disabled** and has no required checks.
- [ ] Optional: `claude mcp add codeit-jira --scope user -- ~/projects/codeit/.venv/bin/codeit-jira-mcp` to use jira-mcp in your own sessions.
- [ ] Review and merge the M2 PR.

## Session 5: M3 (Planner)

### Done

- M2 merged (PR #3). `Protect_Main` is active, with `python` and `gitleaks` as required checks.
- M3 on branch `m3-planner`:
  - `backends/`: Claude Code chat mode (`--tools ""`, `--json-schema`, stdin prompt, API keys stripped); OpenRouter over httpx with model rotation, dead-model marking and a schema fallback; routing and per-role keys
  - `agents/planner.py` (draft with one retry, pure payload mapping, apply with rollback), `planner_schema.py`, `planner_run.py`
  - `prompts/planner/` and `templates/planner/`; `codeit.prompts` with prompt hashes
  - `codeit plan <md|json> [--dry-run] [--epic] [--repo]`; runs recorded in `runs`
  - `docs/samples/sample-plan.md` (6 features)
  - `db.upgrade` repairs databases created before the ADR-0006 fix; it fixed the owner's `data/codeit.db`
  - ADR-0007, PRD v1.4
- Live: the first dry run gave 12 good stories but no dependencies. After a prompt fix, 13 stories with sensible links. About $0.18 per plan (estimated, on the subscription).
- Checks: 306 unit tests pass, 98% coverage. `LIVE=1`: 9/9 pass, including the M3 acceptance test (real tickets in Agent Draft with links, then deleted).

### Pending on the owner

- [ ] Optional: `OPENROUTER_KEY_OPS` in `.env` to enable the Planner's fallback, and review the free model list in `config/config.yaml`.
- [ ] Review and merge the M3 PR.

## Session 6: M4 (sandbox and Claude agentic backend)

### Done

- M3 merged (PR #4). The owner set `CLAUDE_CODE_OAUTH_TOKEN` and an OpenRouter key.
- **ADR-0008:** one OpenRouter key (`OPENROUTER_API_KEY`, old names still read) and model lists overridable from `.env` (`OPENROUTER_MODELS_<NAME>`, `OPENCODE_MODEL`, `CLAUDE_MODEL`). `config validate` shows the effective models.
- **M4** on branch `m4-sandbox` (ADR-0009):
  - `sandbox/Dockerfile`: Playwright 1.63 noble plus gh, jq, uv, Claude Code 2.1.282, OpenCode 1.18.32; user `agent` UID 1000 with no extra groups
  - `sandbox/containers.py` (hardened containers, streaming exec), `clone.py` (mirror plus per-ticket clones, rework rebase, bot identity), `runner.py`, `gc.py`; `git.py`
  - `backends/claude_code_agent.py` (stream-json transcripts, statuses, parking from `rate_limit_event`), `backends/state.py` (`backend_state`)
  - CLI: `codeit sandbox build|smoke|gc`
- Verified: a container reaches jira-mcp on WSL's 127.0.0.1 through `host.docker.internal`, which closes ADR-0004's open question.
- Found and fixed: host-side rebases failed with no committer identity (the machine has no global git identity), and any rebase error was reported as a conflict.
- Checks: 352 unit tests pass, 98% coverage. `LIVE=1`: all pass, including `sandbox smoke` (transcript saved) and a containerized agent reading a ticket through jira-mcp with a coder run token.

### Notes for the owner

- With no `CLAUDE_MODEL`, containers use Claude Code's default (Sonnet 5), while the host Planner uses your personal default (Opus 5.5). Set `CLAUDE_MODEL` if you want one model everywhere.
- Rename `OPENROUTER_KEY_REVIEWER` to `OPENROUTER_API_KEY` in `.env` whenever convenient; both work.

### Pending on the owner

- [ ] Before M4.5: create the GitHub repo `codeit-sandbox-app`, and fine-grained tokens `GITHUB_TOKEN_AGENT` (contents and pull requests read/write on that repo only) and `GITHUB_TOKEN_READONLY`.
- [ ] Review and merge the M4 PR.
